# locally_embedded (deprecated)

Superseded by `rag_prep/` (ingestion) and `Agent persona/src/tools/rag.py` (retrieval).
These files used a local embedding model before the project moved to Gemini embeddings +
ChromaDB. Kept only as history; nothing in the simulation imports them.
