# Copy to .env and fill in. NOTE: the value must have the VAR= prefix -
# a file containing only a bare key is silently ignored by python-dotenv.
OPENROUTER_API_KEY=sk-or-v1-...
# Only needed for dense (ChromaDB + Gemini embeddings) retrieval:
GOOGLE_API_KEY=...
